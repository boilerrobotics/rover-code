<?xml version="1.0"?>
<launch>

  <!-- Load joint controller configurations from YAML file to parameter server -->
  <rosparam file="$(find brc_arm_config)/config/ros_controllers.yaml" command="load"/>

  <!-- Load the controllers -->
  <!-- <node name="controller_spawner" pkg="controller_manager" type="spawner" respawn="false"  -->
    <!-- output="screen" args="arm_position_controller "/> -->
   <include file="$(find brc_arm_hardware_interface)/launch/brc_arm_position_trajectory_controllers.launch" />

</launch>
